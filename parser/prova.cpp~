#include <stdio.h>
#include <vector>

using namespace std;

class scemo {
	public:
		vector<int> cose;
		int ano;
		scemo() ano=0;

	};

int main() {
	scemo me;
	printf("Taglia: %d\n", me.cose.size());
	me.cose.push_back(1);
	printf("Ho aggiunto 1.\n"); 	
	return 1;
}
